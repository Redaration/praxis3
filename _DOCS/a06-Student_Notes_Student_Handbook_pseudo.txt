FUNCTION generate_all_speaker_notes(outline_data, max_slides):
    PREPARE slide content for batch processing
    CREATE prompt for LLM
    CALL LLM to generate speaker notes in batch
    PARSE response and RETURN notes

FUNCTION add_speaker_notes_to_presentation(pptx_file, slide_notes_dict):
    LOAD presentation
    FOR each slide:
        ADD speaker notes from dictionary
    SAVE presentation

FUNCTION process_presentation_with_notes(outline_data, pptx_file, max_slides):
    GENERATE speaker notes
    IF successful, ADD notes to presentation

FUNCTION main():
    GET paths and CHECK for files
    PARSE outline
    CALL process_presentation_with_notes